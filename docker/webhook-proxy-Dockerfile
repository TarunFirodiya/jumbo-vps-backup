FROM node:22-alpine
RUN apk add --no-cache docker-cli
WORKDIR /app
COPY package.json ./
RUN npm install --production
COPY server.js ./
EXPOSE 3001
ENV PORT=3001
ENV DOCKER_HOST=unix:///var/run/docker.sock
CMD ["node", "server.js"]
